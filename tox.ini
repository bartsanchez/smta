[tox]
envlist = py3,flake8

[testenv]
deps = pipenv
commands =
    pipenv install --dev
    pipenv run python smta/manage.py test

[testenv:flake8]
commands =
    pipenv install --dev
    pipenv run flake8 --exclude */migrations/*,settings.py smta
